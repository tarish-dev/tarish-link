[workspace]
resolver = "2"
members = ["crates/tlink", "crates/tlink-hal", "crates/tlink-session", "crates/tlink-cli", "crates/tlink-inject", "crates/tlink-shim"]

[workspace.package]
version = "0.3.0"
edition = "2021"
license = "MIT"

[profile.release]
debug = true          # captures are debugged with perf and gdb; symbols are cheap
