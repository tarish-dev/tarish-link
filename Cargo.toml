[workspace]
resolver = "2"
members = ["crates/libawdl", "crates/libawdl-hal", "crates/libawdl-cli", "crates/awdl-inject"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[profile.release]
debug = true          # captures are debugged with perf and gdb; symbols are cheap
