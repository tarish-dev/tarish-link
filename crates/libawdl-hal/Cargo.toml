[package]
name = "libawdl-hal"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The contract between an AWDL implementation and the radio underneath it."

[dependencies]

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
